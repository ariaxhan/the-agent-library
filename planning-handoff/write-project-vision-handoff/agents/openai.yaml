interface:
  display_name: "Write Vision Handoff"
  short_description: "Create VISION and flow docs"
  default_prompt: "Use $write-project-vision-handoff to turn this plan into implementation handoff docs."

policy:
  allow_implicit_invocation: true
