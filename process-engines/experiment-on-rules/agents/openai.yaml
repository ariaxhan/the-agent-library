interface:
  display_name: "Experiment On Rules"
  short_description: "Test workflows as hypotheses"
  default_prompt: "Use $experiment-on-rules to test whether this rule or workflow is actually working."

policy:
  allow_implicit_invocation: true
