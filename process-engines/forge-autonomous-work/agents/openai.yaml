interface:
  display_name: "Forge Autonomous Work"
  short_description: "Iterate work until checks pass and it survives"
  default_prompt: "Use $forge-autonomous-work to take this task end to end with competing approaches, acceptance checks, stress tests, and a final verdict."

policy:
  allow_implicit_invocation: true
